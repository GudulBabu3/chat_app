name: Deploy to NPN-old

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest and restart
        run: |
          cd /home/oem/chat-app
          git pull origin main
          npm ci --production
          pm2 restart chat-app